plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.14'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'workingdays-api'
            version = '1.1.14'

            from components.java

            pom {
                name = 'Working Days'
                description = 'Working Days is a simple tool for getting the number of working days in a month. It returns the number of working days in the given month.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/workingdays-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/workingdays-api.git'
                    url = 'https://github.com/apiverve/workingdays-api'
                }
            }
        }
    }
}
